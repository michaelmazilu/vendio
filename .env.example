# OpenAI API key for real listing description generation (optional).
# Without this, /api/generate-listing uses a deterministic fallback.
OPENAI_API_KEY=

# Optional: override the vision model (default: gpt-4.1-mini)
# OPENAI_VISION_MODEL=gpt-4.1-mini
